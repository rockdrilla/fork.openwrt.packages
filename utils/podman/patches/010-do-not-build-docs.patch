--- a/Makefile
+++ b/Makefile
@@ -235,7 +235,7 @@ GVPROXY_VERSION=$(shell $(GO) list -m -f
 default: all
 
 .PHONY: all
-all: binaries docs
+all: binaries
 
 .PHONY: binaries
 ifeq ($(GOOS),freebsd)
@@ -869,7 +869,7 @@ rpm-install: package  ## Install rpm pac
 	/usr/bin/podman info  # will catch a broken conmon
 
 .PHONY: install
-install: install.bin install.remote install.man install.systemd  ## Install binaries to system locations
+install: install.bin install.remote install.systemd  ## Install binaries to system locations
 
 .PHONY: install.remote
 install.remote:
